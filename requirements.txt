requests>=2.31
pandas>=1.5
numpy>=1.24
matplotlib>=3.7
folium>=0.14
pyarrow>=12.0
shapely>=2.0
beautifulsoup4>=4.12
pyyaml>=6.0
# Database & API
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
fastapi>=0.110
uvicorn>=0.27
jinja2>=3.1
# Shared WattCarbon grid-data toolkit (private repo).
# Local dev resolves via the developer's git auth; Cloud Run build uses a
# GitHub deploy token (see Dockerfile + section 7 of deploy/cloud-run-dominion.txt).
# No extras here: dominion-api only uses wcgrid.utilities (config loader) and
# the packaged ISO refdata JSON files. The ingestion/docparse/viz extras
# pull in anthropic/pdfplumber/openpyxl/etc that the request path never touches.
wcgrid @ git+https://github.com/wattcarbon/wcgrid.git@main
